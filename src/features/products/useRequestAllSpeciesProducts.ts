import { useLazySearchSpeciesQuery } from "@/api/apb.service";

export function useRequestAllSpeciesProducts(skip) {
    /* const [trigger] = useLazySearchSpeciesQuery();
    console.log("HERE", skip);
    

    void trigger({
      q: 'allspecies',
    }); */
}